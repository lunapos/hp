-- ============================================================
-- LunaPOS βテストデータリセット
-- テナントID: b0000000-0000-0000-0000-000000000001
--
-- 実行方法:
--   psql -f reset_beta.sql
--   または Supabase SQLエディタで実行
--
-- 注意: このスクリプトはβテストデータのみを削除します。
-- stores テーブルの CASCADE 削除により関連データが全て消えます。
-- ============================================================

-- CASCADE で全関連データ（devices, rooms, tables, casts, visits, payments等）も削除
DELETE FROM stores WHERE id = 'b0000000-0000-0000-0000-000000000001';

-- 確認メッセージ
DO $$
BEGIN
    RAISE NOTICE 'βテストデータの削除が完了しました。seed_beta.sql で再投入できます。';
END
$$;
